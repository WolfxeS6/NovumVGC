diplomatic_decisions = {
trade_with_country = {
	
		icon = DI_nationalism
	
		available = {
			is_subject = no
			has_government = social_liberal 
			is_EU_member = yes
		}
		visible = {
		has_completed_focus = SOV_EU_trade
		original_tag = SOV
		is_EU_member = yes
		}

		target_trigger = {
			NOT = { has_war_with = SOV }
			is_EU_member = yes
			exists = yes
			NOT = { original_tag = SOV }
		}
		
		complete_effect = {
			random_list = {
				50 = {
					random_owned_state = {
						add_building_construction = {
							type = infrastructure
							level = 1
							instant_build = yes
						}
					}
				}
                25 = {
					add_offsite_building = { type = industrial_complex level = 1 }
				}
			}
			FROM = {
				country_event = SOV_focus_event.1 
			    increase_trade_influence_by_2 = yes
				add_opinion_modifier = {
					target = ROOT
					modifier = trade_mission
				}
			}
		}
		
		cost = 100
		fire_only_once = yes
	
	}
}