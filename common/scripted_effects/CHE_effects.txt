CHE_random_insurgent_event_effect = {
	random_list = {
		10 = {	#FALLBACK
			modifier = {
				factor = 0
				has_country_flag = CHE_event_100_fired_flg
			}
			country_event = chechnya.100
			SOV = { country_event = chechnya.100 }
			set_country_flag = CHE_event_100_fired_flg
		}
		10 = {
			modifier = {
				factor = 0
				has_country_flag = CHE_event_101_fired_flg
			}
			country_event = chechnya.101
			set_country_flag = CHE_event_101_fired_flg
		}
		10 = {
			modifier = {
				factor = 0
				has_country_flag = CHE_event_102_fired_flg
			}
			country_event = chechnya.102
			set_country_flag = CHE_event_102_fired_flg
		}
	}
}

CHE_trust_calibrate = {
	clamp_variable = {  # The variable will be kept between these levels by calling this each time the variable is changed.
        var = CHE_russian_trust_var
        min = 0
        max = 100
    }
}





