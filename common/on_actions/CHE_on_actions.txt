on_actions = {
	# ROOT is capitulated country, FROM is winner
	on_capitulation = {
		effect = {
			if = {
				limit = {
					ROOT = {
						original_tag = CHE
					}
					SOV = { has_country_flag = CHE_russia_accepted_subjugation_flg }
				}
				CHE = {
					set_country_flag = CHE_resistance_start_flg
					load_focus_tree = {
						tree = chechen_focus
						keep_completed = yes
					}
					activate_decision = CHE_entry_insurgency_decision
				}
				SOV = {
					country_event = chechnya.6
				}
			}
		}
	}
}
