decisions_ASE = {
	ASEAN_Development = {
		visible = {
			country_exists = SIA
		}
		
		available = { 
			OR = {
			is_in_faction_with = SIA
			is_in_faction_with = INS
			is_in_faction_with = SIG
			is_in_faction_with = PHI
			is_in_faction_with = MAY
			}
		}
		icon = industry
		
		cost = 200
		days_re_enable = 180
		
		complete_effect = {
			add_stability = 0.03
			country_event = {
			hours = 8
			id = Thailand.20
						}
					}
		
		ai_will_do = {
			base = 2
		}
	}
	
	ASEAN_Infrastructure = {
		visible = {
			country_exists = SIA
		}
		
		available = { 
			OR = {
			is_in_faction_with = SIA
			is_in_faction_with = INS
			is_in_faction_with = SIG
			is_in_faction_with = PHI
			is_in_faction_with = MAY
			}
		}
		icon = industry
		
		cost = 75
		days_re_enable = 120
		days_remove = 30
		
		complete_effect = {
			add_stability = 0.03
			FROM = {
				every_owned_state= { 
				add_extra_state_shared_building_slots = 1
				add_building_construction = { 
					type = infrastructure 
					level = 1
					instant_build = yes 
						} 
					}
			
				}
			}
		ai_will_do = {
			base = 3
		}
	}
	
	ASEAN_Economic_Boost = {
		visible = {
			country_exists = SIA
		}
		
		available = { 
			OR = {
			is_in_faction_with = SIA
			is_in_faction_with = INS
			is_in_faction_with = SIG
			is_in_faction_with = PHI
			is_in_faction_with = MAY
			}
		}
		icon = economy
		
		cost = 120
		days_re_enable = 183
		
		complete_effect = {
			add_stability = 0.15
			add_war_support = -0.05
			add_ideas = Tourism_Boom
				}
		 remove_effect = {
            remove_ideas = Tourism_Boom
			   }		
		days_remove = 180
		days_re_enable = 180
		ai_will_do = {
			base = 2
		}
	}
	
	ASEAN_consumer_goods_system = {
		visible = {
			country_exists = SIA
		}
		
		available = { 
			OR = {
			is_in_faction_with = SIA
			is_in_faction_with = INS
			is_in_faction_with = SIG
			is_in_faction_with = PHI
			is_in_faction_with = MAY
			}
		}
		icon = mefo
		
		cost = 150
		
		complete_effect = {
			add_stability = 0.02
			add_ideas =  ASEAN_consumer_goods_system 
				}
		days_remove = 140
		days_re_enable = 180
          remove_effect = {
            remove_ideas = ASEAN_consumer_goods_system 
			   }
			   
		ai_will_do = {
			base = 2
		}
	}
	
}